# AGENTS.md

## 1. 角色与职责

- `orchestrator`：拆解任务、分配资源、合并验收。
- `backend-agent`：后端 API、数据模型、服务逻辑。
- `frontend-agent`：前端页面、交互、图表与图谱。
- `probe-agent`：探针采集、扫描、上报、降级控制。
- `compliance-agent`：合规、安全、许可证、审计检查。
- `qa-agent`：单测、集成、性能验证与报告。
- `devops-agent`：CI/CD、部署、发布与回滚流程。

## 2. 任务执行协议

- 输入：需求条目 + 约束 + 验收标准。
- 输出：变更文件、测试结果、风险说明。
- DoD：代码通过 + 测试通过 + 合规通过 + 文档更新。
- 禁止事项：越权改动非授权目录；绕过合规与测试门禁。

## 3. 并行协作规则

- 单任务建议 1~3 个 Agent 并行。
- 单文件同一时刻只能被一个 Agent 修改。
- 跨模块任务采用父子 Agent 结构：
  - 父 Agent 定义接口契约与验收标准。
  - 子 Agent 仅实现指定子域。

## 4. 模型分流策略

- 高复杂度任务使用高能力模型（如架构、跨文件重构、复杂调试）。
- 轻量任务使用经济模型（如检索、样板代码、文档整理）。
- 经济模型连续失败两次自动升级到高能力模型。

## 5. 合规硬规则

- 不得在代码和日志中写入明文敏感信息。
- 所有数据层操作优先 ORM 参数化查询。
- 探针和平台交互必须遵循加密与鉴权规范。
- SBOM 生成必须符合 `docs/sbom` 的字段规范与层级结构。

## 6. 发布前检查清单

- [ ] 需求映射已更新到 `PLANS.md`
- [ ] 变更说明已补充到相关文档
- [ ] 单元测试/集成测试通过
- [ ] 合规检查通过（密钥、协议、许可证、审计）
- [ ] 风险项有明确缓解与回滚策略

